#! /bin/bash

# This pack is valid for apps with the bark-service gem installed
# if [[ -n $(bundle show | grep bark-service) ]]; then
#   exit 0
# else
#   exit 1
# fi

echo "-----> DETECT $1"
exit 0
